<%# encoding: utf-8 %>

$(document).ready(function() {

	$("#e2").select2({
      						minimumInputLength: 2,
      						tokenSeparators:[','],
      						multiple: true,
      						tags:function (query) {
        											if (!query) return;
        												var q = query.term;
     													 ajax: { 	url: "/skills_refresh",
									dataType: 'json',
									type: 'GET',
									data: function (term, page) {
														return {
																	q: term, 
																	page_limit: 1
																};
												},
									results: function (data, page, query) { 
										           alert(data);
										           return {results:  data };
												}
							}
  											      }
    });




});
