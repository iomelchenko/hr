<%= form_for(@skilllt) do |f| %>
  <% if @skilllt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skilllt.errors.count, "error") %> prohibited this skilllt from being saved:</h2>

      <ul>
      <% @skilllt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
